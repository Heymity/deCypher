name: .NET Core

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.301
    - name: Install dependencies
      run: dotnet restore
      working-directory: ./deCypherTest
    - name: Build
      run: dotnet build -define:GITHUB_TESTS --configuration Release --no-restore
      working-directory: ./deCypherTest
    - name: Test
      run: dotnet test -define:GITHUB_TESTS --no-restore --verbosity normal
      working-directory: ./deCypherTest
